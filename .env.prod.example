# .env.prod
# Copy this file to .env.prod and fill in your production values.
# Do NOT commit your actual .env.prod file to version control.

# Database Configuration (MariaDB)
# Use a strong, random password in production
DB_PASSWORD=YOUR_STRONG_DB_PASSWORD_HERE

# Redis Configuration
# Use a strong, random password for Redis in production
REDIS_PASSWORD=YOUR_STRONG_REDIS_PASSWORD_HERE

# Admin Setup Configuration
# Default admin password when the system initializes
ADMIN_PASSWORD=YOUR_STRONG_ADMIN_PASSWORD_HERE

# JWT Secrets (Must be at least 32 characters)
# Generate with: openssl rand -hex 32
JWT_ACCESS_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32
